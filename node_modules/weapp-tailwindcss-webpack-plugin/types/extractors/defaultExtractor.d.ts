interface IMockedContext {
    tailwindConfig: {
        separator: string;
        prefix: string;
    };
}
export declare function defaultExtractor(context: IMockedContext): (content: string) => string[];
export {};
