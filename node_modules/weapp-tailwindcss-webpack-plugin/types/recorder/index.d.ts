export interface IChange {
    oldValue: string;
    newValue: string;
}
export declare class Recorder {
    changes: IChange[];
    constructor();
    push(record: IChange): void;
    clear(): void;
    forEach(callbackfn: (value: IChange, index: number, array: IChange[]) => void, thisArg?: any): void;
}
