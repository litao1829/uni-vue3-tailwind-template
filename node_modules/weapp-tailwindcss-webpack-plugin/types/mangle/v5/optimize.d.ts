import type { IManglePluginOptions } from "../../types";
import { Compiler, Compilation, sources } from 'webpack';
declare const optimizer: (compiler: Compiler, compilation: Compilation, opts: IManglePluginOptions) => (assets: {
    [index: string]: sources.Source;
}) => void;
export default optimizer;
