import type { IManglePluginOptions } from "../../types";
import { compilation, Compiler } from 'webpack';
declare const optimizer: (compiler: Compiler, compilation: compilation.Compilation, opts: IManglePluginOptions) => (chunks: compilation.Chunk[]) => void;
export default optimizer;
